[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
vulnerability = "deny"
unmaintained = "warn"
yanked = "warn"

# Allowed advisories with documented rationale


[[advisories.ignore]]
id = "RUSTSEC-2026-0097"  # rand 0.8 unsound — transitive via statrs→nalgebra
reason = "Transitive dependency via statrs→nalgebra. No rand API is called directly in kam. Upgrade blocked on statrs releasing a version with rand 0.9. statrs 0.18.0 is the current latest and still pins rand 0.8.5."

[[advisories.ignore]]
id = "RUSTSEC-2024-0436"  # paste 1.0.15 unmaintained — transitive via simba
reason = "Transitive dependency via nalgebra→simba→paste. No paste API is called directly in kam. Upgrade blocked on nalgebra→simba update chain."
