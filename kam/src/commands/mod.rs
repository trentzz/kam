//! CLI subcommand implementations.
//!
//! Each module corresponds to one `kam` subcommand and exposes a single
//! `run_*` function that is called from `main`.

pub mod assemble;
pub mod call;
pub mod index;
pub mod models;
pub mod pathfind;
pub mod run;
