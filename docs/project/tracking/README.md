# tracking

Kanban task board for kam. Tasks are markdown files organised into `todo/`, `in_progress/` (implicit — active tasks listed in `open_questions.md`), and `done/`.

Use `docs/project/devmanual/task-tracking.md` for full instructions on creating, updating, and closing tasks.
