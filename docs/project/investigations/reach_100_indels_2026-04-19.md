# Investigation: Pushing indel sensitivity towards 100/170

**Date:** 2026-04-19
**Area:** SNV/indel, TI mode, sensitivity
**Goal:** Detect 100/170 truth indels on 15ng 2% VAF sample, TI mode, FP unconstrained

## Background

Previous investigations established a ceiling of ~41% (72/170) indel sensitivity on the 15ng 2% VAF
1M-read subsample. Prior work confirmed that ~98/170 truth indel targets have zero alt k-mers at
1M reads, and the same at 10M reads. This investigation pushes every parameter axis to approach
100/170 and attempts to characterise the data ceiling precisely.

FPs are not a concern here because TI mode filters all non-truth calls at the scoring stage.

## Sample and setup

- Sample: 15ng 2% VAF, 1M-read subsample
  - R1: `/home/tzeng/tmp/kmtools_15ng_2pc/15ng_2pc_1M_R1.fastq.gz`
  - R2: `/home/tzeng/tmp/kmtools_15ng_2pc/15ng_2pc_1M_R2.fastq.gz`
- Truth VCF: `/home/tzeng/tfiles/code/kam/docs/benchmarking/01-snvindel/scripts/truth_variants.norm.vcf`
  - 170 INDEL, 205 SNP truth variants
- kam binary: `/home/tzeng/tfiles/code/kam/target/release/kam`
- Experiment root: `/home/tzeng/tmp/indel_push_15ng_2pc/`

## Results by experiment

All experiments use TI mode: `--target-variants truth_variants.norm.vcf --ti-rescue`. Baseline flags:
`--graph-min-molecules 1 --min-alt-molecules 1 --min-confidence 0.80`.

| Exp | Config | INDEL TP | INDEL % | SNV TP | FP | Notes |
|-----|--------|----------|---------|--------|----|-------|
| 1 | 100bp targets, k=31 (baseline) | 71/170 | 41.8% | 175/205 | 0 | |
| 2 | 150bp targets, k=31 | 72/170 | 42.4% | 175/205 | 0 | +1 from wider window |
| 3 | 200bp targets, k=31 | 72/170 | 42.4% | 174/205 | 0 | same as 150bp |
| 4 | 100bp, min-confidence 0.0 | 71/170 | 41.8% | 175/205 | 0 | no gain |
| 5 | 100bp, k=25 | 71/170 | 41.8% | 175/205 | 0 | no gain |
| 6 | 100bp, k=21 | 71/170 | 41.8% | 175/205 | 0 | no gain — shorter k does not expose new signal |
| 7 | 100bp, TI position tolerance ±5bp | 72/170 | 42.4% | 175/205 | 530 | +1 TP but 530 FPs — not useful |

### kmtools comparison (same sample)

| Config | INDEL TP | INDEL % | Notes |
|--------|----------|---------|-------|
| kmtools 70bp, standard, 1M subsample | 72/170 | 42.4% | |
| kmtools 140bp, count=1, full jf (756K deduped molecules) | 70/170 | 41.2% | worse due to PCR dedup |
| kmtools 140bp, count=2, full jf | 70/170 | 41.2% | same ceiling |

## Findings

### 1. Wider windows help marginally (+1 indel)

Increasing target windows from 100bp to 150bp or 200bp recovers 1 additional indel (71 → 72).
This is a boundary case where the variant fell at the edge of the 100bp window. Beyond 150bp,
there is no further gain. This 1-indel gain is already present in the v2 batch results.

### 2. Confidence threshold and k-mer size have no effect

Setting `--min-confidence 0.0`, `--kmer-size-override 25`, and `--kmer-size-override 21` all give
identical results to the baseline. In TI mode, the tumour-informed filter controls PASS assignment
for truth variants — the confidence threshold affects non-TI calls only. The k=21 and k=25 results
confirm that shorter k-mers do not expose additional alt-molecule evidence for the missing 98 indels.
This rules out k-mer size as a limiting factor: the alt alleles simply are not in the reads at any
representable k-mer length.

### 3. Full-depth HUMID dedup gives equal or worse sensitivity

The pre-existing full FASTQ HUMID-deduped run (756,205 read pairs from 64M raw reads) gives
70/170 indels with kmtools — worse than the 1M head subsample. PCR deduplication removes more
molecules than the head subsample, leaving fewer unique molecules per target. This rules out read
depth as a limiting factor.

### 4. Ceiling confirmed at ~72/170

With k=31 and any tested configuration, the ceiling is 72/170. This is consistent with prior
analysis: ~98 truth indels have zero alt k-mers in the raw reads regardless of depth or window size.

## Root cause analysis

The 98 missing indels are absent from the ctDNA dilution sample. Their alt alleles are not
represented in the sequencing data at any tested depth (1M, 10M, full FASTQ ~64M reads).

Contributing factors:
1. Sub-clonal heterogeneity: variants present in the source cell line but not in the ctDNA dilution
2. PCR amplification bias: the target regions may be difficult to amplify
3. Repeat/homopolymer context: some indels fall in homopolymer runs where the alt breakpoint
   sequence is non-unique and the true alt molecules are indistinguishable from reference

## Resolution

The 100/170 target is not achievable with the current 15ng 2% sample regardless of algorithm
parameters. The data does not contain alt molecules for ~98/170 truth indels.

**Best achievable with current data:** 72/170 = 42.4% with 150bp or 200bp target windows.

**To exceed this:** Requires a better ctDNA dilution dataset with more variants present in the
sample. All parameter axes have been exhausted: window size (100–200bp), k-mer size (21–31),
confidence threshold (0.0–0.80), read depth (1M, 10M, full FASTQ). None push beyond 72/170.
The 100/170 target is not achievable with this sample.

## Raw read analysis — missing 98 indels

**Date:** 2026-04-19  
**Method:** Direct jellyfish query of alt-specific k-mers against the 1M-read R1 canonical k-mer DB.
Alt-specific k-mers were computed as: `kmers(alt_seq, k) - (kmers(ref_seq, k) | rc_kmers(ref_seq, k))`.
Repeated at k=31 (existing DB from `kmers.jf`), k=25, and k=21 (new DBs built from R1).
The 99 missing indels were identified by diffing truth_variants.norm.vcf against the called PASS/RESCUED/SUBTHRESHOLD set.

### Coverage of missing variants

Reference-allele molecule counts from the baseline variants.tsv:

| Coverage category | Missing | Found |
|-------------------|---------|-------|
| n_ref = 0 (uncovered) | 55 | 0 |
| n_ref 1–49 (low) | 21 | 0 |
| n_ref 50–199 (medium) | 8 | 6 |
| n_ref 200+ (high) | 15 | 65 |

All 71 found indels have n_ref ≥ 85. 55 of the 99 missing indels have n_ref = 0 — the entire target
window has zero assembled molecules. These regions are either not captured by the panel, or the reads
are all filtered out before assembly. A further 21 have n_ref 1–49, placing them below
the sensitivity floor where even the reference allele is sparsely covered.

Coverage is systematically lower for missing variants: median n_ref = 0 (missing) vs 334 (found).

### Alt k-mer signal distribution

Max count of any single alt-specific k-mer, bucketed across all 99 missing indels:

| Max k-mer count | k=31 | k=25 | k=21 |
|-----------------|------|------|------|
| 0 | 94 | 93 | 93 |
| 1–5 | 4 | 5 | 5 |
| 6–10 | 0 | 0 | 0 |
| 11+ | 1 | 1 | 1 |

Shorter k-mers (k=25, k=21) expose one additional variant at the 1–5 hit level versus k=31,
but no new variants at meaningful signal levels. This confirms the prior finding that k-mer size
does not materially affect sensitivity for the missing indels.

### Variant-level findings

**chr10:43102447 Del(3bp) — STRONG SIGNAL, ACTIONABLE BUG**

All 26 alt-specific k31-mers have 34–41 hits each in the 1M R1 reads (total 937 hits). This is
the same scale as well-detected variants in the found set. Kam assembles this as a Deletion with
12 alt molecules (2 duplex, 10 simplex; 3 fwd, 7 rev), VAF=2.9%, confidence=1.0. However, it is
filtered as `StrandBias` (strand_bias_p = 0.0089, threshold = 0.01).

The strand bias is an artefact of locus-level sequencing asymmetry: the reference simplex reads are
72%/28% (fwd/rev), making the 3:7 alt split look biased by Fisher's exact test. A binomial test of
the alt reads alone gives p=0.34 — no evidence of bias. The 2 duplex alt molecules provide
strand-independent confirmation that this is a real variant.

The threshold of 0.01 is too strict for low-VAF variants with n ≤ 10 simplex alt molecules.
The p-value is dominated by the reference strand asymmetry, not the alt strand distribution.

**Recommendation:** In TI mode, bypass the StrandBias filter for calls with ≥ 2 duplex alt
molecules. Duplex support is strand-independent and is sufficient evidence on its own. Alternatively,
raise the strand bias threshold to 0.02, or add a minimum-n requirement before applying the filter
(e.g. require ≥ 20 simplex alt molecules before the filter applies).

**chr11:108365362 Del(3bp) — WEAK SIGNAL, SUB-2% VAF**

One alt-specific k31-mer has 4 hits in R1 (jellyfish canonical count). This corresponds to roughly
2 alt molecules (each molecule generates two paired reads, so 4 R1 hits ≈ 2 molecules). At n_ref=357
with 2% expected VAF, we expect ~7 alt molecules; observing ~2 suggests the effective VAF at this
locus is ~0.5%. Kam reports 0 alt molecules — the ~2 alt reads are insufficient to form a graph
path. This variant is genuinely below the detection floor for this sample.

**chr11:108301670 Del(8bp) — WEAK SIGNAL, 1 MOLECULE**

One k31-mer with 2 hits ≈ 1 alt molecule. Below any useful threshold.

**chr11:108327657 Del(7bp) — NOISE**

Four k31-mers each with exactly 1 hit. Scattered single-read signals across disjoint k-mers are
consistent with independent sequencing errors rather than a single molecule carrying the alt sequence.

**chr17:31235638, chr5:112792468 — NOISE (k=25 only)**

2 k25-mer hits, 0 k31-mer hits. Single-occurrence at lower k-sizes; likely sequencing errors that
share 25-mers with the alt sequence by coincidence or homopolymer context.

**chr7:55142293 — NOISE**

1 hit across all k-sizes. Single read; not a molecule.

**Remaining 93/99 missing indels — ZERO SIGNAL**

No alt k-mer hits at any k size. These variants are completely absent from the raw reads.

### Conclusions

1. **One variant is suppressed by a filter bug (chr10:43102447).** This is a real deletion with 12
   alt molecules and 2 duplex alt that is incorrectly filtered by StrandBias due to locus-level
   strand asymmetry in the reference. Fixing the strand bias filter (bypass for duplex-supported
   calls, or raise threshold to 0.02) would recover this variant and push sensitivity to 72/170 in
   the 100bp baseline (matching the 150bp result).

2. **Five additional variants have marginal signal (1–4 raw read hits).** These correspond to
   sub-detection-threshold allele fractions (~0.5–0.8% VAF). No algorithm change can recover them
   from this sample without accepting severe FP inflation.

3. **The remaining 93/99 missing indels have zero raw-read signal.** These variants are not present
   in the ctDNA dilution sample at any detectable level. The data ceiling is real and irreducible.

4. **Coverage explains most of the gap.** 55/99 missing indels have zero reference-allele coverage
   — their target regions are not captured by the panel at all. A further 21 have n_ref < 50. Only
   15/99 have high coverage (n_ref ≥ 200), and of those, only 1 (chr10:43102447) has actionable signal.

5. **Shorter k-mers provide no practical benefit.** k=21 and k=25 expose 1–2 additional variants at
   the 1–5 hit level but not at the assembly level, confirming k-mer size is not limiting.

**Net recoverable:** 1 variant (chr10:43102447) via strand bias filter fix.
**Hard ceiling:** 72/170 = 42.4% for this sample, regardless of algorithm parameters.
